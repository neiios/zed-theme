# zed theme

a custom zed theme based on https://github.com/vitallium/zed-modus-themes

# installation

1. symlink the `themes` directory to `~/.config/zed/themes`:

```bash
ln -sf ~/dev/zed-theme/themes ~/.config/zed/themes
```

2. open zed and select the theme in settings

# license

GPL-3.0
